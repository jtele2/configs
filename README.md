# Joe Caprino's Configs
Run the bash file to setup the shell the way I like. 

## TODO
The following feature need to be added to an ansible script: 
  - Python3 pip install (if needed)
    -- On Ubuntu: sudo apt-get install python3-pip
  - Ansible install
  - zsh install
  - zsh config updates (`.zshrc`, symlinks, etc.)
  - oh-my-zsh install
  - Docker install
  - AWS CLI install

